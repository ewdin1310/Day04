function main(numOne, numTwo) {
    var numOne = 55;
    var numTwo = 0;
    var quotient = numOne / numTwo;
    if(numTwo === 0){
        quotient = "Divide by 0! Cannot by zero!";
    } else {
    //Only change code below this line
    
    //Only change code adove this line
    }
    return quotient;
}
console.log (main(15, 3));
module.exports = main;